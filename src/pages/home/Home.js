import ModalCreatePlan from '../../components/Modal/ModalCreatePlan';
import {useState} from 'react'
function App() {
  // const [visibleCreatePlanModal, setVisibleCreatePlanModal] = useState(true);
  // const [projectInFolder, setProjectInFolder] = useState("Project mới");
  return (
    <div>
      <h1>Home</h1>

      {/* <ModalCreatePlan
        visible={visibleCreatePlanModal}
        setVisible={setVisibleCreatePlanModal}
        defaultName={projectInFolder}
      /> */}
    </div>
  );
}

export default App;
